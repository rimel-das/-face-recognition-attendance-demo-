# Face Recognition Attendance App — Python dependencies
# Install with:  pip install -r requirements.txt
#
# NOTE: face_recognition requires dlib which requires cmake.
# See SETUP_AND_RUN.md for platform-specific installation steps.

# Core dependencies
flask>=2.3.0
opencv-python>=4.8.0
face_recognition>=1.3.0
numpy>=1.24.0

# Utilities
flask-cors>=4.0.0
python-dotenv>=1.0.0
Werkzeug>=2.3.0
