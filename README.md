# -face-recognition-attendance-demo-
Facial recognition attendance system for schools and offices using Python Flask and OpenCV. Real-time face scanning, automatic marking with duplicate prevention, live dashboard, attendance reports with CSV export, and modern dark UI. SQLite database. Windows, Linux, macOS compatible.
